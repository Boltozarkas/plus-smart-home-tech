spring:
  application:
    name: api-gateway
  config:
    import: ""
  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false

app:
  security:
    users:
      - username: ivan
        password: ivan
        roles:
          - USER
      - username: anna
        password: anna
        roles:
          - USER
          - ADMIN